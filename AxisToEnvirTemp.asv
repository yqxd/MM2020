function result = AxisToEnvirTemp(x)
% ------------------------------------
% 根据对应的横坐标计算得到对应环境位置的温度
% x 为所输入的横坐标
% ------------------------------------
    global ReflowOven
    if x <= 25
    else if AxisToBreak(x) 
    else if (x <= 55.5 | (x >= 60.5 & x < 91 | (x >= )))
            result = 182